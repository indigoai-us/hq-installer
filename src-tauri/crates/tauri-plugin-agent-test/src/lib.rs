// Intentionally empty — see Cargo.toml comment block. This crate is a
// vendored placeholder so cargo's manifest resolution doesn't fail on
// fresh checkouts or CI runners that lack the sibling source repo.
